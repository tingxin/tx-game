# AWS SDK for Python - 用于 IAM Identity Center 和其他 AWS 服务
boto3>=1.34.0
botocore>=1.34.0

# HTTP 请求库 - 用于调用 Kiro API 和其他 REST API
requests>=2.31.0

# 配置文件处理
python-dotenv>=1.0.0

# JSON Web Token 处理 - 用于 SSO 令牌验证
PyJWT>=2.8.0

# 加密库 - 用于证书和密钥处理
cryptography>=41.0.0

# XML 处理 - 用于 SAML 响应解析
lxml>=4.9.0

# 日期时间处理
python-dateutil>=2.8.0

# 数据验证
pydantic>=2.0.0

# 命令行界面 (可选)
click>=8.1.0

# 日志处理 (可选)
colorlog>=6.7.0

# 开发和测试依赖 (可选)
pytest>=7.4.0
pytest-mock>=3.11.0
black>=23.0.0
flake8>=6.0.0