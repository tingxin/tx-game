# Kiro 用户添加完整流程说明

## 概述

在 Kiro 系统中添加用户涉及两个层面：**身份认证层**（AWS IAM Identity Center）和**应用层**（Kiro Dashboard）。这两者协同工作，提供完整的用户管理和访问控制。

## 架构关系图

```
┌─────────────────────────────────────────────────────────────┐
│                    用户访问流程                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   用户浏览器     │───▶│  AWS IAM Identity │───▶│ Kiro Dashboard  │
│                │    │     Center       │    │                │
│  1. 访问 Kiro   │    │  2. SSO 认证     │    │ 3. 应用授权     │
│  2. 重定向登录   │    │  3. 返回令牌     │    │ 4. 用户界面     │
└─────────────────┘    └──────────────────┘    └─────────────────┘
                              │                       │
                              ▼                       ▼
                    ┌──────────────────┐    ┌─────────────────┐
                    │  Identity Store  │    │ Kiro 用户数据库  │
                    │  (AWS 用户数据)   │    │ (应用用户数据)   │
                    └──────────────────┘    └─────────────────┘
```

## 详细流程步骤

### 步骤1: AWS IAM Identity Center 身份创建

**目的**: 创建统一的身份认证账户

```python
# 在 AWS Identity Store 中创建用户
response = identity_store_client.create_user(
    IdentityStoreId=identity_store_id,
    UserName="kiro",
    DisplayName="Kiro Assistant",
    Name={
        'GivenName': "Kiro",
        'FamilyName': "Assistant"
    },
    Emails=[{
        'Value': "kiro@company.com",
        'Type': 'work',
        'Primary': True
    }]
)
```

**作用**:
- 🔐 创建 AWS 统一身份
- 📧 设置用户基本信息（姓名、邮箱）
- 🆔 生成唯一的 AWS 用户ID

### 步骤2: AWS 权限分配

**目的**: 配置用户可以访问的 AWS 资源

```python
# 分配权限集给用户
sso_admin_client.create_account_assignment(
    InstanceArn=instance_arn,
    TargetId=aws_account_id,
    TargetType='AWS_ACCOUNT',
    PermissionSetArn=permission_set_arn,
    PrincipalType='USER',
    PrincipalId=user_id
)
```

**作用**:
- 🔑 控制 AWS 资源访问权限
- 🛡️ 设置安全边界
- 📊 定义可用的 AWS 服务

### 步骤3: Kiro Dashboard 用户创建

**目的**: 在应用层面创建用户账户

```python
# 在 Kiro 系统中创建用户
kiro_user_data = {
    "username": "kiro",
    "email": "kiro@company.com",
    "role": "admin",
    "auth_provider": "aws_sso",
    "external_id": "kiro",  # 关联 AWS 用户名
    "workspace_access": ["default"],
    "features": {
        "ai_assistant": True,
        "code_analysis": True,
        "collaboration": True
    }
}
```

**作用**:
- 🎯 设置应用内权限和角色
- 📁 配置工作空间访问权限
- ⚙️ 定义功能模块权限
- 👤 存储用户偏好设置

### 步骤4: SSO 集成配置

**目的**: 建立 AWS 和 Kiro 之间的认证桥梁

```python
# SAML/OIDC 配置
sso_config = {
    "provider": "aws_identity_center",
    "username_attribute": "username",
    "email_attribute": "email",
    "role_attribute": "custom:role",
    "auto_provision": True,
    "user_mapping": {
        "aws_username": "kiro",
        "kiro_username": "kiro"
    }
}
```

**作用**:
- 🔗 建立身份映射关系
- 🔄 启用自动用户同步
- 🎫 配置令牌传递规则

## 两个系统的职责分工

### AWS IAM Identity Center (身份层)
| 功能 | 描述 | 示例 |
|------|------|------|
| **身份认证** | 验证用户身份 | 用户名/密码验证 |
| **单点登录** | 一次登录，多处访问 | 登录后访问所有授权应用 |
| **权限管理** | AWS 资源访问控制 | S3、Bedrock、Lambda 权限 |
| **用户目录** | 统一用户信息存储 | 姓名、邮箱、组织信息 |

### Kiro Dashboard (应用层)
| 功能 | 描述 | 示例 |
|------|------|------|
| **应用权限** | Kiro 功能访问控制 | 管理员、开发者、普通用户 |
| **工作空间** | 项目和资源管理 | 代码仓库、AI 模型配置 |
| **用户体验** | 个性化设置 | 主题、语言、快捷键 |
| **业务逻辑** | 应用特定功能 | 代码分析、AI 对话历史 |

## 用户登录流程

```
1. 用户访问 Kiro Dashboard
   ↓
2. Kiro 检测到未认证，重定向到 AWS SSO
   ↓  
3. 用户在 AWS Identity Center 登录
   ↓
4. AWS 验证身份，生成 SAML/JWT 令牌
   ↓
5. 令牌返回给 Kiro Dashboard
   ↓
6. Kiro 验证令牌，查找对应的应用用户
   ↓
7. 用户成功登录，获得 Kiro 应用权限
```

## 数据同步关系

### 用户信息映射
```python
# AWS Identity Center 用户
aws_user = {
    "UserId": "12345678-1234-1234-1234-123456789012",
    "UserName": "kiro",
    "DisplayName": "Kiro Assistant",
    "Emails": ["kiro@company.com"]
}

# Kiro Dashboard 用户  
kiro_user = {
    "id": "kiro_user_001",
    "username": "kiro",           # 映射到 AWS UserName
    "email": "kiro@company.com",  # 映射到 AWS Email
    "external_id": "kiro",        # 关联 AWS 用户
    "auth_provider": "aws_sso",   # 标识认证来源
    "role": "admin",              # Kiro 应用角色
    "workspaces": ["ws_001"]      # Kiro 工作空间权限
}
```

## 最佳实践

### 1. 用户创建顺序
```
推荐顺序: AWS → Kiro → SSO 配置
原因: 确保身份认证基础设施先就绪
```

### 2. 权限设计原则
```
AWS 权限: 最小化原则，只授予必要的 AWS 资源访问权限
Kiro 权限: 基于角色，根据用户职责分配功能权限
```

### 3. 用户信息同步
```
主数据源: AWS Identity Center (姓名、邮箱等基本信息)
扩展数据: Kiro Dashboard (应用偏好、工作空间等)
```

### 4. 错误处理
```
AWS 用户存在: 跳过创建，继续后续步骤
Kiro 用户存在: 更新映射关系，确保同步
SSO 配置失败: 用户可以手动登录，但失去 SSO 便利性
```

## 总结

这个双层用户管理架构的优势：

1. **安全性**: AWS Identity Center 提供企业级身份认证
2. **灵活性**: Kiro Dashboard 可以独立管理应用权限
3. **用户体验**: 单点登录，用户无需记住多个密码
4. **可扩展性**: 可以轻松集成其他应用和服务
5. **合规性**: 符合企业身份管理和审计要求

通过这种设计，用户在 AWS Identity Center 中的身份变更会自动同步到 Kiro，而 Kiro 的应用权限可以独立管理，实现了身份认证和应用授权的分离。